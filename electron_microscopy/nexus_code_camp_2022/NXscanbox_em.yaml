category: base
doc: |
  Scan box and coils which deflect the electron beam in a controlled manner.
  
  In electron microscopy, the scan box is instructed by the microscope
  control software. This component directs the probe to controlled locations
  according to a scan scheme and plan.
(NXscanbox_em):
  calibration_style:
  center(NX_NUMBER):
    unit: NX_ANY
    # \@units:
    #  enumeration: nm
  flyback_time(NX_FLOAT):
    unit: NX_TIME
  field_of_view(NX_FLOAT):
    unit: NX_LENGTH
  line_time(NX_FLOAT):
    unit: NX_TIME
  pixel_time(NX_FLOAT):
    unit: NX_TIME
  requested_pixel_time(NX_FLOAT):
    unit: NX_TIME
  rotation(NX_FLOAT):
    unit: NX_ANGLE
  ac_line_sync(NX_BOOLEAN):
# NEW ISSUE: build on work of EMglossary with HMC and use duty cycle instead
# NEW ISSUE: make use of and define duty cycle
